<!-- comprobante_template.html -->
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body { font-family: sans-serif; }
        .titulo { text-align: center; font-size: 20px; font-weight: bold; }
    </style>
</head>
<body>
    <div class="titulo">EMPRESA DE TRANSPORTES CHICLAYO S.A.</div>
    <p>R.U.C.: {{ ruc }}</p>
    <p>Cliente: {{ cliente_nombre }} - DNI: {{ cliente_dni }}</p>
    <p>Ruta: {{ ruta }} - Asiento: {{ asiento }}</p>
    <p>Fecha de viaje: {{ fecha_viaje }} - Hora: {{ hora_viaje }}</p>
    <p>Importe: S/ {{ importe }}</p>
</body>
</html>
